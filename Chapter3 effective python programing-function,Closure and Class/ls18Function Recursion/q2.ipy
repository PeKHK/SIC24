#use recursive to calculate pow n of x
x=int(input('Enter x:'))
n=int(input('Enter n:'))
def pow_num(x,n):
    if n == 0:
        return 1
    elif n == 1:
        return x
    else:
        return x*pow_num(x,n-1)
print(pow_num(x,n))